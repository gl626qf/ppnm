using System;
using static System.Console;
using static System.Math;
class main{
	public static void Main(string[] args){
		Write("hello\n");
		foreach(string arg in args)System.Console.Out.WriteLine(arg);
		double[} numbers = input.get_numbers_from_args(args);
}

